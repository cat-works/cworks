[package]
name = "cworks-lua"
description = "Pure mlua wrapper library for CWorks (no FFI exports)"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["rlib"]

[dependencies]
mlua = { version = "0.10.5", features = ["lua54", "vendored"] }
log = "0.4.17"
