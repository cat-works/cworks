{
  "saveAndRun": {
    "commands": [
      {
        "cmd": "cargo run; wasm-pack build --target web wasm -d ../pkg" /*
        "cmd": "wasm-pack build --target web wasm -d ../pkg" /*
        "cmd": "cargo run" /*
        "cmd": "cargo clippy" /*
        "cmd": "cargo test kernel::test::kernel -- --nocapture" /*
        // */
      }
    ]
  }
}